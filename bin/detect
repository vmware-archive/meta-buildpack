#!/usr/bin/env ruby
# Encoding: utf-8
# bin/detect <build-dir>

build_dir = ARGV[0]

$stdout.sync = true
$stderr.sync = true

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require "decorator"

detect(build_dir)
